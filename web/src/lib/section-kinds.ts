/** Mirrors `master_cv.SECTION_KINDS` for editors when no import response is loaded. */
export const DEFAULT_SECTION_KINDS = ["profile", "experience_block", "education", "skills", "projects", "other"] as const;
