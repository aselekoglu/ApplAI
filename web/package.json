{
  "name": "applai-web",
  "private": true,
  "version": "0.1.0",
  "description": "ApplAI web UI. Dev: copy .env.example to .env (optional; VITE_API_URL defaults to http://127.0.0.1:8000 in code). Run FastAPI, then npm run dev (dev server port set in vite.config.ts).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.30.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.11",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^1.3.2",
    "typescript": "^4.9.5",
    "vite": "^2.9.18"
  }
}
